<Window x:Class="TaskPlanner.View.SelectProjectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskPlanner.View"
        xmlns:vm="clr-namespace:TaskPlanner.ViewModel"
        mc:Ignorable="d"
        Title="Task Planner" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#cdcdcd">
    <Window.DataContext>
        <vm:SelectProjectViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <StackPanel/>

        <Border Grid.Row="1" CornerRadius="25" Background="#bcbcbc" Margin="0 10 0 0">
            <ListView Background="Transparent" BorderBrush="Transparent" Margin="25"/>
        </Border>
    </Grid>
</Window>
